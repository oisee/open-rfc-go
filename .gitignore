# Go build and test output
/bin/
*.test
*.out
coverage.txt

# Fuzz corpus entries found locally; committed only with a regression test
testdata/fuzz/

# Local SAP MCP config — contains live credentials, never commit
.mcp.json

# Built CLI binaries and traffic captures (captures may contain credentials)
/rfc-sniffer
*.jsonl
/captures/
/rfc-server
/rfc-lab
/rfc-viewer
/callz
/cmd/callz/
/cmd/oracle/
.rfc.json
